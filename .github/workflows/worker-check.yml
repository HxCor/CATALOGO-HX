name: Worker security check

on:
  push:
    paths:
      - 'worker/**'
      - 'wrangler.toml'
      - '.github/workflows/worker-check.yml'
  pull_request:
    paths:
      - 'worker/**'
      - 'wrangler.toml'
      - '.github/workflows/worker-check.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: JavaScript syntax
        run: node --check worker/index.js
      - name: Wrangler dry-run build
        run: npx --yes wrangler@4 deploy --dry-run --outdir /tmp/catalogo-worker-build
      - name: Verify password hardening markers
        run: |
          grep -q 'PBKDF2_ITERATIONS = 310000' worker/index.js
          grep -q 'Password Hash' worker/index.js
          grep -q '"Contraseña": null' worker/index.js
          grep -q 'SESSION_TTL_SECONDS' worker/index.js
